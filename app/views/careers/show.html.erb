<div class="container">
  <h2 class="category-title"><strong> <%= @career.title %> </strong></h2>
  <h3 class="category-description"><%= @career.description %></h3>

  <div class="row">
    <% @professionals.each do |professional| %>
    <div class="col-xs-12 col-md-6 col-xl-4">
      <%= render "shared/professional_card",  professional: professional  %>
    </div>
    <% end %>
  </div>
</div>
